import { Meta } from '@storybook/addon-docs/blocks';
import TryBadge from '../assets/Badges/TryBadge'
import DeprecatedBadge from '../assets/Badges/DeprecatedBadge'
import Tip from '../assets/Banners/Tip'

import '../style.css'

<Meta title="Modal/Study" />

# The <code>Modal</code> Component

## Version 0

<TryBadge href="/?path=/docs/modal-v0--default" />
<DeprecatedBadge />

Courtesy on LETO (Camille Toulouse)

<div className="subheading">API</div>

```xml
<Modal {...args} isOpen={isOpen} onModalClosed={() => setIsOpen(false)}>
  <ModalButtonClose>x</ModalButtonClose>
  <div>Try clicking outside of the modal!</div>
</Modal>
```

Very similar to <a href="/?path=/story/modal-study--page#version-2">version 2</a>

## Version 1

<TryBadge href="/?path=/docs/modal-v1--default" />

<div className="subheading">API</div>

```xml
<Modal isOpen={isOpen} onClose={() => setIsOpen(false)} title="Title">
  Try clicking outside of the modal or pressing 'Escape'!
</Modal>
```

<div className="subheading">Advantages</div>
<ul>
  <li>Only 1 component</li>
  <li>Just a wrapper over <a href="/?path=/story/modal-study--page#version-2">version 2</a></li>
</ul>

<div className="subheading">Drawbacks</div>
<ul>
  <li>Similar to <a href="/?path=/story/tabs-study--page#version-1">Tabs v1</a> but without the advantages</li>
  <li><code>title</code> is prop</li>
</ul>

## Version 2

<TryBadge href="/?path=/docs/modal-v2--default" />

<div className="subheading">API</div>

```xml
<Modal isOpen={isOpen} onClose={() => setIsOpen(false)}>
  <ModalTitle>My title</ModalTitle>
  <ModalContent>Try clicking outside of the modal or pressing 'Escape'!</ModalContent>
</Modal>
```

<div className="subheading">Advantages</div>
<ul>
  <li>Customizable</li>
</ul>

### Version 2.1

<TryBadge href="/?path=/docs/modal-v2-1--default" />

Exactly like <a href="/?path=/story/modal-study--page#version-2">version 2</a> but using a context

<div className="subheading">Advantages</div>
<ul>
  <li>Can contain &ldquo;non-Modal&rdquo; Elements (e.g. <a href="/?path=/docs/modal-v2-1--with-non-modal">With Non Modal</a>)</li>
</ul>

<div className="subheading">Drawbacks</div>
<ul>
  <li>Uses a context</li>
</ul>
